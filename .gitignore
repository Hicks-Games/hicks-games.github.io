# This project has no build step and no dependencies on purpose.
# Anything below showing up means something went sideways.

node_modules/
package-lock.json
dist/
build/

# Word lists downloaded by tools/build-wordlist.js. The built result
# (words/wordlist.js) is committed; these raw sources are not.
.build/

# OS / editor noise
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/
*.swp
